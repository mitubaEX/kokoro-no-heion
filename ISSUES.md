# Issues — 心の平穏アプリ (Kokoro no Heion)

次に必要な作業の一覧。優先度: **P0**=次にやるべき / **P1**=近いうちに / **P2**=将来。
フェーズはアイデアサマリーの実装フェーズに対応。

ステータス凡例: `todo` / `in-progress` / `done`

---

## フェーズ1（コア機能）の残作業

### #1 先人の教え集を実装する
- 優先度: **P0** ／ フェーズ1 ／ status: `done`
- 背景: 現在 `src/pages/Teachings.tsx` はプレースホルダー。フェーズ1の3本柱の最後。
- 内容:
  - `src/data/teachings.ts` に、出典の確かな言葉のみを構造化して収録（`{ id, text, source, theme[], note }`）。
  - 出典が不確実な引用・誤帰属は載せない（アイデアの「不確かな教えは排除」方針）。パブリックドメイン or 明確な出典に限定。
  - テーマ／悩み別の絞り込み（例: 不安・人間関係・自己受容）と、わかりやすい一言解説。
  - ホームの「今日の一言」と連携（教えへ遷移）。
- 受け入れ条件:
  - [ ] テーマで絞り込んで教えを閲覧できる
  - [ ] 各教えに出典が表示される（出典不明のものは登録しない）
  - [ ] `npm run build` が通る

### #2 永続化基盤（localStorage ラッパ）を用意する
- 優先度: **P0** ／ フェーズ1 ／ status: `done`
- 背景: 設定・お気に入り・日記など今後の機能の土台。プライバシー方針上、端末ローカル保存。
- 内容:
  - `src/lib/storage.ts` に型安全な get/set（JSON・バージョニング・try/catch）を実装。
  - SSR/プライベートモードで例外時もクラッシュしないフォールバック。
- 受け入れ条件:
  - [ ] キー単位で型付き読み書きできる
  - [ ] localStorage 不可環境でも例外を握りつぶして既定値を返す

### #3 設定・再生状態を永続化する
- 優先度: **P1** ／ フェーズ1 ／ status: `done`（#2 に依存）
- 内容: 音量・最後に選んだサウンドスケープ・タイマー既定値・瞑想の既定パターン/長さを保存し、次回起動時に復元。
- 受け入れ条件:
  - [ ] 再読み込み後も音量と最後の選択が復元される

---

## 基盤・品質（フェーズ進行と並行）

### #4 音声有効化まわりの UX を整える
- 優先度: **P1** ／ status: `done`
- 背景: ブラウザ仕様で音は初回ユーザー操作後に有効化（`ctx.resume`）。モバイルでのバックグラウンド復帰や無音時の案内が未整備。
- 内容:
  - 初回は「タップで音が始まります」の控えめな案内。
  - `visibilitychange` でのサスペンド/レジューム挙動を確認・調整。
- 受け入れ条件:
  - [ ] iOS Safari / Android Chrome で初回タップ後に確実に発音する

### #5 アクセシビリティ & モーション配慮
- 優先度: **P1** ／ status: `done`
- 内容:
  - キーボード操作（ナビ・再生ボタン・チップ）と `:focus-visible` の可視化。
  - `prefers-reduced-motion` で呼吸アニメ・装飾アニメを抑制。
  - 主要 interactive 要素の `aria-label` 見直し。
- 受け入れ条件:
  - [ ] キーボードのみで全画面を操作できる
  - [ ] reduced-motion 設定時に過度なアニメが止まる

### #6 自動テストを整備する
- 優先度: **P1** ／ status: `done`
- 内容:
  - Vitest を導入し、純粋ロジックから着手: `formatClock`、`phraseOfTheDay`（日付安定性）、呼吸フェーズ進行ロジック。
  - 可能なら AudioEngine をモックして再生/停止/タイマーの状態遷移をテスト。
- 受け入れ条件:
  - [ ] `npm test` が通る
  - [ ] format / phrases のユニットテストが存在

### #7 PWA 化（オフライン・インストール対応）
- 優先度: **P2** ／ status: `done`
- 内容: `vite-plugin-pwa` 導入、manifest、アイコン、Service Worker でオフライン再生対応。
- 受け入れ条件:
  - [ ] ホーム画面に追加でき、オフラインでも音と瞑想が使える

### #8 アイコン/ブランディングの整備
- 優先度: **P2** ／ status: `done`
- 内容: 絵文字アイコンを SVG コンポーネント化、favicon / OGP 画像、`theme-color` の最終調整。

### #9 依存関係の健全性
- 優先度: **P2** ／ status: `done`
- 内容: `npm audit` の moderate 2件（dev 依存）を確認し、影響範囲を見て対応方針を決める。

---

## フェーズ2（UI改善・パーソナライズ）

### #10 パーソナライズされたおすすめ
- 優先度: **P2** ／ フェーズ2 ／ status: `done`（#2 に依存）
- 内容: 再生・瞑想の利用履歴をローカル集計し、時間帯/頻度に応じたおすすめサウンド・パターンをホームに提示。
- 受け入れ条件:
  - [ ] 利用履歴に基づくおすすめがホームに表示される

---

## フェーズ3（日記・コンテンツ拡充）

### #11 心の日記機能
- 優先度: **P2** ／ フェーズ3 ／ status: `done`（#2 に依存）
- 背景: 現在 `src/pages/Diary.tsx` はプレースホルダー。
- 内容: 気分（絵文字/スケール）＋ひとことメモを日付ごとに記録。一覧・振り返り。すべて端末ローカル保存。
- 受け入れ条件:
  - [ ] その日の気分とメモを保存・編集・削除できる
  - [ ] 過去の記録を一覧で振り返れる
  - [ ] データは端末内のみ（外部送信なし）

### #12 サウンド/瞑想コンテンツの拡充
- 優先度: **P2** ／ フェーズ3 ／ status: `done`
- 内容: サウンドスケープ追加（焚き火・心拍・ホワイトノイズ等）、瞑想ガイドのバリエーション（ボディスキャン等）。

---

## フェーズ4（継続更新・フィードバック）

### #13 フィードバック導線とコンテンツ更新基盤
- 優先度: **P2** ／ フェーズ4 ／ status: `done`
- 内容: アプリ内フィードバック導線、教え/サウンドのデータ駆動更新の仕組み。

---

### おすすめの着手順
1. **#1 先人の教え集**（フェーズ1完了）
2. **#2 永続化基盤** → **#3 設定永続化**
3. **#6 テスト** / **#5 a11y**（品質の底上げ）
4. 以降フェーズ2→3→4
